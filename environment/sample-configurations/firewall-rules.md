# Firewall Rules

VPC firewall rules were overly permissive and relied on solely default configurations.

Allow or default allow rules were configured for multiple protocols and ports.

Including the following:

- 8080
- SSH (tcp: 22)
- Internal
- RDP
- ICMP
